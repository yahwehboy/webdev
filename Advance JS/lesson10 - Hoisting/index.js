function getWeather(){
    return "Today's weather is warm and sunny"
}

console.log(getWeather())

console.log(getNews())  //functions and variables ar moved to the top - hoisted only funtion will work

function getNews(){
    return "A new swimming pool has opened in the town center..."
}

let trafficInfo = 'All roads are busy right now'

console.log(trafficInfo)

//let trafficInfo = 'All roads are busy right now' //is in temporary dead zone
